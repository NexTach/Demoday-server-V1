type Query {
    getMemberById(id: ID!): Member
    getAllMembers: [Member]
}

type Mutation {
    updateMember(id: ID!, phoneNumber: String, password: String, role: String): Member
}

type Member {
    id: ID
    phoneNumber: String
    role: String
}